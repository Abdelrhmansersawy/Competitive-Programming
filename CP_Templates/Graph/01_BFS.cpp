/* 
	########### 0-1 BFS ###########
	Optimzed algrothim from dikjstra can used when weight is (0 , 1)
	Time complexity: O(n + m)
*/
// 0-1 BFS
deque<int> q; //{x , y}
vector<int>> dis(n , INT32_MAX);
q.push_front();
dis[] = 0;
while(!q.empty()){
// if new weight incearse by 1 >> push_back in deque
// if new weight still the same >> push_front in deque
}
